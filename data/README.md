# Assignment data

Each assignment keeps its inputs, reproducible scripts, outputs, and evidence in
its own `assignment-NN/` directory. Generated files must identify their source
and must not be described as real observations unless their provenance supports
that claim.

## Contents

- `assignment-01/`: workspace setup and successful local skill-run evidence.
